---
title: "Find credential history by credential hex value"
description: "Retrieves all credentials that have owned the credential hex value. Expect at most 1 valid/active credential and the rest are revoked"
openapi: "POST /api/accesscontrol/findCredentialHistoryByCredentialHexValue"
icon: "magnifying-glass"
---

Retrieves all credentials that have owned the credential hex value. Expect at most 1 valid/active credential and the rest are revoked

<Note>
The parameters and response fields below are automatically generated from the OpenAPI specification.
</Note>
