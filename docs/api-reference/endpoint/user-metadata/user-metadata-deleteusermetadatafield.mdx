---
title: "Delete user metadata field"
description: "Deletes the specified user's value stored for the specified template, removing the custom field value while preserving the template definition."
openapi: "POST /api/user/metadata/deleteUserMetadataField"
icon: "user"
---

Deletes the specified user's value stored for the specified template, removing the custom field value while preserving the template definition.

<Note>
The parameters and response fields below are automatically generated from the OpenAPI specification.
</Note>
