---
title: "Find user metadata fields"
description: "Returns the org template metadata fields and the queried users stored values for those fields if they've been set, providing complete metadata information fo..."
openapi: "POST /api/user/metadata/findUserMetadataFields"
icon: "user"
---

Returns the org template metadata fields and the queried users stored values for those fields if they've been set, providing complete metadata information for a specific user.

<Note>
The parameters and response fields below are automatically generated from the OpenAPI specification.
</Note>
